<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- to draw a line only at the bottom, we stroke a rectangle whose other edges are offscreen… -->
    <item android:left="-2sp" android:right="-2sp" android:top="-2sp"
        android:bottom="1sp">
        <shape>
            <stroke android:color="@android:color/white" android:width="1sp" />
        </shape>
    </item>
</layer-list>
